const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function main() { const result = await prisma.project.deleteMany({ where: { slug: "cricket" } }); console.log("Deleted:", result); } main().catch(e => console.error(e)).finally(() => prisma.$disconnect());
