{
  "name": "gamecenter-admin-panel",
  "version": "1.0.0",
  "description": "GameCenter Admin Panel - Full Stack (Frontend + Backend)",
  "private": true,
  "scripts": {
    "build": "npm install --prefix backend && npm --prefix backend run db:generate && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node backend/src/server.js",
    "dev": "node backend/src/server.js",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
